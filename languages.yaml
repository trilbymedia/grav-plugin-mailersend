en:
  PLUGIN_MAILERSEND:
    API_TOKEN: API Token
    DEFAULTS: Defaults
    DEBUG: Debug Mode
    DEBUG_HELP: No sending, only logging to grav.log
    TO: To/Recipient
    FROM: From
    CC: CC
    BCC: BCC
    EMAIL_FORMAT: "Use the `name-addr` format: `Name <email@address.org>`. Comma separated for multiple addresses"
